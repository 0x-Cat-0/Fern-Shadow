# Plant Recorder App - 开发文档

## 代码审查结果 (2026-04-09)

### 审查范围
全部源代码，重点关注数据库操作、状态管理和UI逻辑。

### 发现并修复的问题

#### 1. 硬编码占位图URL不一致
**问题**：多处使用 `https://picsum.photos/` 作为默认图片，与Repository层处理逻辑不一致。

**Repository处理方式**：
```typescript
// GroupRepository.create
coverImagePath: dto.coverImagePath || '',

// IndividualRepository.create
coverImagePath: dto.coverImagePath || '',
```

**修复位置**：
- `EditGroupScreen.tsx:152-154` - 改为空字符串
- `CreateIndividualScreen.tsx:263` - 改为空字符串
- `CreateGroupScreen.tsx:392` - 改为使用鹿角蕨.png本地图片
- `EditIndividualScreen.tsx:336` - 改为空字符串
- `EditIndividualScreen.tsx:658` - 改为使用鹿角蕨.png
- `IndividualDetailScreen.tsx:802` - 改为使用鹿角蕨.png

**建议**：后续如需统一修改默认图片，可创建常量：
```typescript
// constants/index.ts
export const DEFAULT_PLANT_IMAGE = require('../assets/icons/鹿角蕨.png');
export const DEFAULT_PLANT_IMAGE_URL = ''; // 用于需要URL的场景
```

### 代码逻辑概述

#### 数据库层 (database/)
- **index.ts**: SQLite初始化，表结构：groups, individuals, group_individuals, records
- **GroupRepository**: 支持创建/更新分组、添加/移除个体关联
- **IndividualRepository**: 支持创建/更新植物、浏览量计数
- **RecordRepository**: 记录图片路径存储为JSON字符串

#### 屏幕组件 (screens/)
- **MainScreen**: 主页，显示全部植物和分组，提供排序功能
- **IndividualDetailScreen**: 植物详情，时间线布局显示记录
- **GroupDetailScreen**: 分组详情，支持拖动调整弹窗高度
- **CreateGroupScreen/CreateIndividualScreen**: 创建页面
- **EditGroupScreen/EditIndividualScreen**: 编辑页面

#### 工具函数 (utils/)
- **ImageStorage.ts**: 图片复制到文档目录，永久URI管理

### 潜在问题（未修复，需关注）

1. **浏览量统计**: `IndividualRepository.create` 未传入viewCount，使用默认值0
   - 如果需要初始化浏览量，需要确认业务逻辑

2. **图片EXIF时间戳**: 已实现读取逻辑，但需在真机测试验证

3. **日期分组逻辑** (`CreateIndividualScreen.tsx:219-237`):
   ```typescript
   const dateKey = `${date.getFullYear()}-${date.getMonth()}-${date.getDate()}`;
   ```
   - 同一天不同时刻的照片会被分到同一组，这是设计行为

### 架构亮点

1. **Repository模式**: 清晰的数据访问层分离
2. **数据库迁移**: 支持添加新列和索引
3. **图片管理**: 永久URI与临时URI分离，清理逻辑完整
4. **PanResponder**: 拖动调整弹窗高度的用户体验优化

### 待优化项（建议）

1. 创建 `DEFAULT_COVER_IMAGE` 常量统一管理默认图片
2. 考虑使用 `Date.toISOString().split('T')[0]` 替代手动拼接日期key
3. 图片选择器考虑添加进度提示

### 测试建议

1. Android真机测试EXIF时间戳读取
2. 测试创建植物时选择多张同日期/不同时日期的图片
3. 测试分组添加/移除个体的各种场景
